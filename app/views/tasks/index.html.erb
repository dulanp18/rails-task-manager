<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><input type="checkbox" name="<%= task.id %>" value="true"><%= link_to task.title,  task_path(task)%></li>
  <% end %>
</ul>

<%= link_to "Create New Task",  new_task_path %>
